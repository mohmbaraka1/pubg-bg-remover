import BackgroundRemover from "./components/BackgroundRemover.jsx";

// المرحلة الأولى فقط: صفحة واحدة، لا توجيه (routing)، لا صفحات إضافية.
export default function App() {
  return <BackgroundRemover />;
}
