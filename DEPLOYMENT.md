# دليل النشر

## 1) تجربة محلية بحاويات (قبل أي استضافة حقيقية)

```bash
cp .env.example .env   # وحط قيم Supabase الحقيقية
docker compose up --build
```

يفتح على `http://localhost:8080`. أول طلب إزالة خلفية بياخذ وقت أطول لأنه
يحمّل أوزان SAM2 (~GB) و BiRefNet من الإنترنت أول مرة فقط (تُحفظ بـ volume
دائم `backend_weights`، ما تتحمّل من جديد بكل إعادة تشغيل).

⚠️ Docker مو مثبّت بهالبيئة اللي أشتغل فيها، فما قدرت أبني/أشغّل الحاويات
فعلياً هنا للتأكد الكامل - جرّبها عندك بجهازك، ولو طلعت مشكلة ابعتلي رسالة
الخطأ وأصلّحها.

## 2) الاستضافة الفعلية - خياراتك

الباك إند (FastAPI + SAM2 + BiRefNet + YOLO) ثقيل ويفضّل GPU للسرعة
المعقولة (على CPU يشتغل لكن كل صورة تاخذ ثواني لدقائق بدل أقل من ثانية).

| الجزء | خيارات شائعة |
|---|---|
| الفرونت (static) | Vercel / Netlify / Cloudflare Pages - مجاني وبسيط، يبني من `frontend/` |
| الباك إند (CPU) | Railway / Render / Fly.io - أسهل، أرخص، أبطأ بالمعالجة |
| الباك إند (GPU) | RunPod / Vast.ai / Lambda Cloud / VPS فيه GPU - أسرع بكثير، أغلى وأعقد شوي |

## 3) خطوات عامة (بغض النظر عن مزوّد الاستضافة)

**الفرونت:**
1. اضبط متغيرات البيئة بلوحة تحكم الاستضافة: `VITE_SUPABASE_URL`,
   `VITE_SUPABASE_ANON_KEY`, و `VITE_API_URL` (رابط الباك إند الحقيقي بعد
   نشره، مثلاً `https://api.yourdomain.com`) - إلا لو رح تستخدم بروكسي
   (nginx/Cloudflare) يمرّر `/api` لنفس الدومين، وقتها اتركه فاضي.
2. أمر البناء: `npm run build` (مجلد `frontend/`)، مجلد الإخراج: `dist/`.

**الباك إند:**
1. ابنِ ونشر `backend/Dockerfile` (كل الاستضافات المذكورة فوق تدعم نشر من
   Dockerfile مباشرة).
2. اضبط متغيرات البيئة:
   - `CORS_ALLOWED_ORIGINS=https://yourdomain.com` (دومين الفرونت الحقيقي -
     **لا تسيبه `*` بالإنتاج**).
   - `BG_REMOVER_DEVICE=cuda` لو عندك GPU، وإلا `cpu` (أو اتركه بدون ضبط،
     "auto" الافتراضي يكتشف تلقائياً).
3. اربط تخزين دائم (volume/disk) بمسار `/app/weights` جوة الحاوية - وإلا كل
   نشر جديد يعيد تحميل أوزان النماذج (GB) من الإنترنت من الصفر.

## 4) ملاحظات أمان قبل الإطلاق الفعلي

- `CORS_ALLOWED_ORIGINS` لازم يكون دومين الفرونت الحقيقي فقط، مو `*`.
- تأكد `frontend/.env` نفسه ما ينرفع لأي مكان عام (هو أصلاً بـ `.gitignore`
  الحين) - القيم اللي فيه (anon key) مصممة تكون عامة أصلاً فمو خطر حرج، بس
  ما تحط فيه مستقبلاً أي `service_role key` أو سر حقيقي.
- راجع سياسات RLS بجداول Supabase (خصوصاً `assets`, `accounts`,
  `subscriptions`) قبل الإطلاق للجمهور - تأكد كل مستخدم يشوف بياناته هو بس.
